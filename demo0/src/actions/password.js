import { useReducer } from 'react'
import { reducer, initialValues } from '../reducers/password'

const password = () => {
  // const [state, dispatch] = useReducer(reducer, initialValues);

  // const changeVisible = (visible) => dispatch({ type: 'MODAL_VISIBLE', payload: { visible: visible } })

}

export default password

